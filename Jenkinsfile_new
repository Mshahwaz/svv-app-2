@Library('my-shared-lib@main') _
pipeline {
    agent any
    environment {
        REPO = "app2-flask"
        TAG = "${BUILD_NUMBER}"
        PORT = "5050"
    }
    stages{
        stage('Build'){
            steps{
                buildimage()
            }
        }
        stage('Deploy'){
            steps{
                deploycontainer()
            }
        }
    }
}